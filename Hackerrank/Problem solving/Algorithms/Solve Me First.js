       function solveMeFirst(a, b) {
            return a+b   
        }
  
        var a = parseInt(readLine());
        var b = parseInt(readLine());;

        var res = solveMeFirst(a, b);
        console.log(res);
